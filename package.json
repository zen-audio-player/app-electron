{
  "name": "zen-audio-player-electron",
  "version": "0.1.0",
  "main": "main.js",
  "dependencies": {
    "electron-prebuilt": "^1.2.7"
    },
   "scripts": {
    "pre-lint": "npm install eslint",
    "lint": "eslint *.js",
    "pretest": "npm run pre-lint && npm run lint",
    "start": "electron ."
  }
}

