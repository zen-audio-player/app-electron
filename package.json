{
  "name": "zen-audio-player-electron",
  "version": "0.1.0",
  "main": "main.js",
  "dependencies": {
    "electron-prebuilt": "^1.2.7"
    },
   "scripts": {
    "pre-lint": "eslint install",
    "lint": "eslint *.js",
    "pretest": "npm pre-lint && npm run lint",
    "start": "electron ."
  }
}

